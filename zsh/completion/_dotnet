#compdef dotnet

_dotnet() {
  local -a completions
  local line="${(j: :)words[1,CURRENT]}"
  completions=(${(f)"$(dotnet complete --position ${#line} "${line}" 2>/dev/null)"})
  compadd -- $completions
}

_dotnet "$@"
